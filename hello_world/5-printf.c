#include <stdio.h>

/**
 * main- Prints the phrase the task that was given to us.
 *
 * Return: 0 always.
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
