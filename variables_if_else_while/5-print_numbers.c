#include <stdio.h>

/**
 * main- prints all single digit numbers of base 10
 * starting from 0
 *
 * Return: 0 always
 */
int main(void)
{
	/* var declaration */
	int i;

	/* code */
	for (i = 0; i < 10; i++)
		printf("%i", i);
	printf("\n");
	return (0);
}
